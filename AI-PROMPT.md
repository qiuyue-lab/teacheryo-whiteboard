# 给 AI 的说明

> 这份文档是给**你的 AI 助手**看的。
> 想让 AI 帮你跑起来、部署上线、或者改成你想要的样子 ——
> 找到对应的一段，**整段复制给它**就行。

---

## 0. 先说结论：这东西到底是什么

**它不是网站服务，是一份可以直接拿走的课堂工具源码。**

你拿到的是一个 GitHub 仓库。它属于开源项目里很常见、但新人容易误会的一类：
**零构建 · 可自托管的网页应用**（zero-build, self-hosted web app）。

| | 它**是** | 它**不是** |
|---|---|---|
| 分发形式 | 一个 Git 仓库（clone / fork 走） | 不是 npm 包、不是 Docker 镜像 |
| 运行形式 | 一组静态文件，双击 `index.html` 就能跑 | 不需要编译、打包、`npm install` |
| 数据形式 | 浏览器本地存储，**或**你自建的云数据库 | 没有官方服务器，作者不存你的数据 |
| 使用形式 | 每人是自己的独立副本 | 不是共享的 SaaS，没有统一登录 |

**一句话对外介绍**：

> 这是一个开源的小工具，不是产品。你 clone 到电脑上双击就能用；
> 想让学生手机扫码参与，就自己申请一个云环境把配置填进去。
> 每个人用的是自己独立的一份，互不影响。

---

## 1. 给它"背景"：下面这段最重要

不管你后面要做什么，**先把这段发给 AI**，它就不会犯低级错误了。

```
背景：我本地有一个「TeacherYO 课堂互动工具」项目，是一个零构建的纯前端网页应用。

技术形态：
- 纯 HTML + CSS + 原生 JS，没有 npm、没有 webpack/vite、没有构建步骤。双击 index.html 就能运行。
- 目录结构：
  index.html      老师端（建课程、发互动、看大屏）
  student.html    学生端（扫码进来提交）
  css/style.css   全部样式
  js/common.js    公共组件（无限画布、便签渲染、一键整理）
  js/db.js        数据层入口，启动时自动判断用本地还是云端
  js/store.local.js  本地模式实现（浏览器 localStorage）
  config.js       配置模板（envId 留空 = 本地模式）
  config.local.js 我的私有云配置，不进 Git
  vendor/         CloudBase Web SDK（已本地化）
  cloudbase/migrations/  建表 SQL

数据层是双模式的：envId 留空 → 用 localStorage；填了值 → 连腾讯云 CloudBase 的
PostgreSQL。两种模式对外暴露的方法名、字段名（snake_case）完全一致，
所以页面代码不区分模式。

对外的数据接口是 TY.db.*，主要有：
  listCourses / getCourse / createCourse / updateCourse / deleteCourse
  listBoards / getBoard / getBoardByCode / createBoard / updateBoard /
  deleteBoard / moveBoard / archiveBoard / reopenBoard
  listSubmissions / submit / toggleLike / removeSubmission / updateSubmission

互动类型有 5 种：notes(便签墙) / choice(选择题) / blank(填空题) / cloud(词云) / material(材料学习)

请你先读一下 README.md 和 AI-PROMPT.md，确认你理解了结构，再等我下一步指令。
```

### 如果你是网页版 AI（ChatGPT / Claude / 豆包…）

它读不到你本地的文件，所以除了上面那段，还要**额外说明这一点**：

```
注意：你没法访问我的本地文件。如果你需要看某个文件的内容，
请直接告诉我文件名，我会贴给你，不要凭猜测编造代码。
```

---

## 2. 按你的目标选一段

### 🟢 目标 A：我只是想先跑起来看看

**最适合**：本地电脑上试用、备课、一个人演示。

```
我想在本地把这个项目跑起来看看。

请告诉我最省事的启动方式，并且：
1. 直接双击 index.html 和起一个本地服务器（python3 -m http.server）有什么区别？
   哪个更稳？
2. 跑起来之后，我怎么验证「本地模式」是正常的？看哪里能确认？
3. 我想演示完整课堂流程（老师发 + 学生答），只有一台电脑应该怎么操作？
4. 如果打开是白屏，帮我列一个排查清单，按可能性排序。
```

---

### 🔵 目标 B：部署到网上，能发链接给别人

**最适合**：想让朋友点开就能看，不需要学生真的提交。

```
我想把这个项目部署成在线版，能通过链接访问。

我的情况是：<GitHub 账号情况 / 有没有域名 / 用不用腾讯云>

请给我一份步骤清单，并且重点告诉我：
1. 部署到 GitHub Pages 之后，数据和本地模式一样吗？学生扫码提交我这边能看到吗？
   （我需要知道这个版本的能力边界，别说成"能用了"）
2. 部署过程中最容易失败的一步是什么？怎么判断成功了？
3. 更新代码之后怎么重新部署？
```

> 💡 **给 AI 的关键提醒**（可以一起贴上）：
> 「在线版只是把页面放到网上，数据仍存在每个访问者自己的浏览器里。
> 学生扫码提交，我的大屏是看不到的。请你在回答里明确这一点，不要含糊。」

---

### 🟣 目标 C：让它变成"学生真的能扫码参与"

**最适合**：真实课堂使用。这是唯一需要花钱/注册的一步。

```
我想把这个项目接到我自己的云数据库，让学生手机扫码提交的内容
实时出现在我的大屏上。

我打算用腾讯云 CloudBase。请给我完整步骤：
1. 需要开通哪些服务？大概什么量级会产生费用？个人课堂会花多少钱？
2. 建表 SQL 在 cloudbase/migrations/ 里，那些行级安全策略（RLS）是干什么的？
   按默认配置，知道 envId 的人能做到哪些操作？我需要改成什么样才安全？
3. config.local.js 该怎么写？为什么只能填 Publishable Key 不能填 Secret Key？
4. 部署完成后，我该怎么验证"学生提交 → 大屏出现"这条链路真的通了？
   给我一个最少步骤的验证方法。
```

> ⚠️ **这段一定要让 AI 回答清楚安全问题**。前端直连数据库的架构下，
> `envId` 和公开密钥必然是公开的。别跳过。

---

### 🟠 目标 D：我想改功能 / 加一种新的互动类型

**最适合**：想改成自己学校的样子，或者加个新玩法。

```
我想修改这个项目：<具体想改什么，比如"加一种新的互动类型"、
"把便签墙换成别的样式"、"加一个导出 Excel 的功能">

动手之前，请先告诉我：
1. 这个改动会涉及哪些文件？每个文件里负责什么？
2. 有哪些看起来能改、但改了会连带出问题的地方？
3. 改完之后要测哪些路径才能确认没弄坏别的功能？

（我的底线是：这个项目必须保持"双击 index.html 就能跑"，
不要引入任何构建工具或 npm 依赖。）
```

---

### 🔴 目标 E：出问题了，我要报 bug

**调 bug 最忌讳说"不工作了"**。按这个格式说，AI 一次就能定位：

```
我遇到一个问题，请帮我排查。

【我做了什么】<具体操作，比如"在便签墙上点了一键整理，选了按颜色分组">
【我期望什么】<比如"便签按颜色排好">
【实际发生什么】<比如"什么都没发生，控制台报 xxx 错">
【在哪个模式】本地模式 / 云端模式
【复现概率】每次都这样 / 偶尔 / 只见过一次
【我看到的报错】<浏览器控制台（F12）里的红字，请原样贴进来>
【已经试过什么】<比如"刷新了、换浏览器了，都没用">

如果信息不够，请列出还需要我提供什么，不要直接猜原因。
```

> 📌 **一个通用提醒**：只要你改了样式（`css/style.css`），
> 一定要**强制刷新**（Mac：`⌘ + Shift + R`）再看效果，
> 否则浏览器会一直用缓存里的旧样式 —— 很多人因此白折腾半天。

---

## 3. 兜底的约束（防止 AI 把项目改坏）

如果 AI 开始乱来 —— 加构建工具、改接口名、装一堆依赖 —— 把这段甩给它：

```
请遵守这个项目的以下约束，不要越界：

1. 不引入 npm / webpack / vite / TypeScript 等任何构建工具或依赖。
   这个项目必须保持"双击 index.html 就能跑"。
2. 不改变 TY.db.* 的方法名和字段名（snake_case）。
   js/store.local.js 和 js/db.js 的云端实现必须一直保持一致，
   否则本地模式和云端模式会行为不一致。
3. 不往 Git 里提交 config.local.js，也不要把任何真实密钥写进代码。
4. 改完之后请给我一个验证方法（最好是能实测的），不要只口头说"改好了"。
5. 不确定的地方先问我，不要自己编造这个项目里不存在的文件或接口。
```

---

## 4. 最后：这三种说法是**错的**

| ❌ 别这么说 | ✅ 换成这么说 | 为什么 |
|---|---|---|
| "帮我部署这个网站" | "这是我 clone 下来的一个静态前端项目，我想部署到 XX" | 它没有官方服务器，不是 SaaS，AI 会理解错 |
| "npm install 然后跑起来" | "这是零构建项目，双击 index.html 就能跑" | 它没有 `package.json`，AI 会瞎编一套 |
| "网站打不开了，你修一下" | 按第 2 节「目标 E」的四要素描述 | AI 看不到你的屏幕，"不工作了"等于零信息 |

---

**许可**：MIT。随便改、随便用，包括商用。唯一要求是保留版权声明。
