/* ============================================================
 * TeacherYO · 配置
 * ============================================================
 * 【默认状态】api 留空 = 本地模式。
 *   数据保存在浏览器里，不用注册任何账号，双击 index.html 就能用。
 *   适合：试用、备课、单机演示。
 *
 * 【多人协作】想让「学生手机扫码提交 → 大屏实时汇聚」，才需要填下面
 *   的 api 数据服务地址（Cloudflare Worker + D1）。
 *   示例：api: 'https://teacheryo-api.<你的子域>.workers.dev'
 *
 * 【安全提醒 · 重要】
 *   这个文件会随网页一起被任何人下载到，所以这里只放公开信息
 *   （Worker 地址是公开的），任何密钥都不放进此文件。
 *
 *   想本地覆盖配置（不进 Git）：写到 config.local.js 即可
 *   （该文件已被 .gitignore 忽略）。
 * ============================================================ */
window.TY_CONFIG = {
  api: '',
  baseUrl: ''
};
